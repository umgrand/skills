---
description: Create an on-brand Betashares HTML presentation deck
argument-hint: [topic or brief]
allowed-tools: [Read, Write, Edit, Bash, Agent, Glob, Grep]
---

Create a Betashares HTML presentation using the `create-presentation` skill.

**User's request:** $ARGUMENTS

Follow the `create-presentation` skill workflow. If no topic was provided, ask the user what the presentation is about before proceeding.
